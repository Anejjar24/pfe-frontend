import React, { useEffect, useRef } from "react";
export default function EditorToolbar() {
  return (
    <div>
      <button>Save</button>
      <button>Load</button>
      <button>Clear</button>
    </div>
  );
}
